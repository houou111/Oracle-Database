

SELECT LONG_WAITS/IO_COUNT, FILENAME
FROM V$BACKUP_ASYNC_IO
WHERE LONG_WAITS/IO_COUNT > 0
ORDER BY LONG_WAITS/IO_COUNT DESC;



SELCET EFFECTIVE_BYTES_PER_SECOND,ISCRETE_BYTES_PER_SECOND
FROM V$BACKUP_SYNC_IO

SELCET EFFECTIVE_BYTES_PER_SECOND 
FROM V$BACKUP_ASYNC_IO


SELECT LONG_WAITS/IO_COUNT, FILENAME
FROM V$BACKUP_ASYNC_IO
WHERE LONG_WAITS/IO_COUNT > 0
ORDER BY LONG_WAITS/IO_COUNT DESC;